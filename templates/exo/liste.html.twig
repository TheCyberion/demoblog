{% extends "base.html.twig" %}
{% block body %}
<a class="btn btn-success mt-2" href="{{path('voiture_new')}}">Ajouter une voiture</a>
<table class="table"> 
   <thead>
     <tr>
       <th>Marque</th>
       <th>Modèle</th>
       <th>Prix</th>
       <th>Description</th>
       <th>Editer</td>
       <th>Supprimer</th>
     </tr>
    <tbody>
     {% for voiture in voitures %}
          <tr>
            <td>{{voiture.marque}}</td>
            <td>{{voiture.modele}}</td>
            <td>{{voiture.prix}} €</td>
            <td>{{voiture.description|raw}}</td>
            <td>
               <a href="{{ path('voiture_edit', {id: voiture.id})}}" class="btn btn-info">Editer</a>
            </td>
            <td>
               <a onclick="return confirm ('voulez-vous supprimer cet article ?')" href="{{path('voiture_delete', {id: voiture.id})}}" class="btn btn-danger">Supprimmer</a>   

    </tbody>  

       {% endfor %}
</table>
{% endblock %}